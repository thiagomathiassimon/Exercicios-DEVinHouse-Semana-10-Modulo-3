import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  root: {
    width: 250,
    backgroundColor: "#282A36cc",
    color: "white",
    position: "fixed",
    bottom: 6,
    right: 8,
  },
  title: {
    color: "white",
    fontSize: 14,
    marginBottom: -5,
  },
});
